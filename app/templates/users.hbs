{{#paper-list}}
  {{#paper-subheader}}Users{{/paper-subheader}}
  {{#each model as |user|}}

    {{#paper-item class="md-2-line"}}
      <div class="md-list-item-text">
        <h3>{{user.name}}</h3>
        <p>{{user.email}}</p>
      </div>
    {{/paper-item}}

  {{/each}}
{{/paper-list}}
{{outlet}}
